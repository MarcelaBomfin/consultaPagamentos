
stages:
  - test
  - report

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

before_script:
  - echo "Preparando ambiente"

test_job:
  stage: test
  script:
    - echo "Rodando testes"
    - npm install
    - npm test
  artifacts:
    paths:
      - reports/
    when: always

report_job:
  stage: report
  script:
    - echo "Gerando relatório"
  artifacts:
    paths:
      - reports/
